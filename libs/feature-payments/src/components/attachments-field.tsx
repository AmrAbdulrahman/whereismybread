// Moved to @wib/ui (shared with feature-debts). Kept as a re-export so the
// existing import sites in this lib don't churn.
export {
  AttachmentsField,
  type AttachmentUploadResult,
} from '@wib/ui';
