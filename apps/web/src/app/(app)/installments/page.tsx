import { Placeholder } from '../../_components/placeholder';
export const metadata = { title: 'Installments' };
export default function Page() {
  return (
    <Placeholder title="Installments" phase="Phase 3">
      Fixed payments split over time, ending on a date or a total amount.
    </Placeholder>
  );
}
