import { Placeholder } from '../../_components/placeholder';
export const metadata = { title: 'Calendar' };
export default function Page() {
  return (
    <Placeholder title="Upcoming payments" phase="Phase 2">
      List (grouped by date), Calendar, and Year views of every upcoming
      payment.
    </Placeholder>
  );
}
