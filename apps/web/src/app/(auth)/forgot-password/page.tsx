import { Placeholder } from '../../_components/placeholder';
export const metadata = { title: 'Reset password' };
export default function Page() {
  return <Placeholder title="Reset your password" phase="Phase 1" />;
}
